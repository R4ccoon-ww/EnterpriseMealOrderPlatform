<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webox.mapper.OrderMapper">

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, total_amount, delivery_date, meal_period, delivery_address, status)
        VALUES (#{userId}, #{totalAmount}, #{deliveryDate}, #{mealPeriod}, #{deliveryAddress}, #{status})
    </insert>

    <insert id="insertOrderItems">
        INSERT INTO order_item (order_id, menu_item_id, name, price, quantity) VALUES
        <foreach collection="items" item="i" separator=",">
            (#{i.orderId}, #{i.menuItemId}, #{i.name}, #{i.price}, #{i.quantity})
        </foreach>
    </insert>

    <select id="findByUserId" resultType="com.webox.entity.Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findByIdAndUserId" resultType="com.webox.entity.Order">
        SELECT * FROM orders WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="findItemsByOrderId" resultType="com.webox.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

</mapper>
